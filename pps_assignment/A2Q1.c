#include<stdio.h>
int main()
{
    inta;
    printf("Enter a year");
    sacnf("%",&a)
    if(a%4==0)
    {
        printf("%d is a leap year")
    }
    else
{
    printf("%d is not a leap year") 
}
